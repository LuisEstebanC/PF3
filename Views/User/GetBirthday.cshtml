@{
    
    Database conexion = Database.getInstancia();

      Microsoft.Data.Sqlite.SqliteDataReader lista = conexion.GetBithdayData(00);
      while (lista.Read())
      {
        @lista.GetString(0)
      }
  }